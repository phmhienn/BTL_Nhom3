<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BTL_Nhom3">
        <activity
            android:name=".item_profile_option"
            android:exported="false"
            android:label="@string/title_activity_item_profile_option"
            android:theme="@style/Theme.BTL_Nhom3" />
        <activity android:name=".feature_auth.ui.RegisterActivity" />
        <activity android:name=".feature_cart.ui.CartActivity" />
        <activity android:name=".feature_cart.ui.CheckoutActivity" />
        <activity android:name=".feature_menu.ui.SearchActivity" />
        <activity android:name=".feature_menu.ui.FoodDetailActivity" />
        <activity android:name=".feature_auth.ui.LoginActivity" /> <!-- Login là launcher mới -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>